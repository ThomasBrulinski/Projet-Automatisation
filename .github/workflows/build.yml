name: Build
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  sonarqube:
    name: SonarQube
    # Utilisation d'Ubuntu pour supporter gunicorn et les scripts shell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      
      # Ajout de la configuration Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' 

      # Installation des dépendances Python
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend-traitement/app/requirements.txt

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          # Remplacez les valeurs ci-dessous par celles de votre compte SonarQube
          args: >
            -Dsonar.organization=thomasbrulinski
            -Dsonar.projectKey=ThomasBrulinski_Projet-Automatisation
            -Dsonar.sources=.

            -Dsonar.verbose=true
